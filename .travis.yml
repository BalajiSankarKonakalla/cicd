language: java
jdk:
  - oraclejdk8

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-pip python-setuptools
  - sudo pip install awscli

script:
  - ./gradlew build
  - aws s3 cp learning-ci-cd.jar s3://ci-cd-test-bucket/learning-ci-cd-${TRAVIS_BUILD_NUMBER}.jar

